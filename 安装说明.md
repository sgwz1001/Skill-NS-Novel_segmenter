# 小说分段助手 · 安装说明

一个跨 Agent 应用通用的技能包。把网文长段落切成短段节奏，顺手校对错别字和标点。

## 目录结构

```
小说分段助手/
├── SKILL.md              技能主文件（AI 读它来干活）
├── 安装说明.md            本文件
├── 网页版.html            纯本地网页工具，双击即用，秒出结果（无需 Agent）
├── scripts/
│   └── fenduan.py        分段与校对引擎（纯标准库，无需装依赖）
└── references/
    ├── 错别字表.md        网文高频错别字对照
    └── 分段细则.md        边界情况处理规则
```

## 一、安装到 WorkBuddy

把整个 `小说分段助手` 文件夹复制到：

```
个人级（所有项目可用）：  C:\Users\<你的用户名>\.workbuddy\skills\
项目级（仅当前项目）：    <项目目录>\.workbuddy\skills\
```

重启对话即可生效。

## 二、安装到 Claude Code / Q Card 等

绝大多数 Agent 应用都遵循同一套 Agent Skills 规范（`SKILL.md` + YAML 头部），
放进各自的 skills 目录即可：

| 应用 | 安装位置 |
|---|---|
| Claude Code | `~/.claude/skills/` |
| Cursor | `~/.cursor/skills/`（或项目内 `.cursor/skills/`） |
| Q Card / 其他 | 查看该应用的「技能 / Skills / 扩展」设置项，多数支持选文件夹导入 |

**如果某个应用要求技能名必须是英文**：把文件夹改名为 `xiaoshuo-fenduan`，
同时把 `SKILL.md` 第 2 行的 `name: 小说分段助手` 改成 `name: xiaoshuo-fenduan`。
两处必须一致。技能内容和输出仍然是全中文。

## 三、怎么用

启用技能后，**在对话框里粘贴正文，什么都不用说，直接发送。**

技能会自动产出三份东西：

| 产物 | 用途 |
|---|---|
| `分段正文.txt` | 纯净正文，无序号，开头带一行标题。全选复制 → 粘贴进起点作家助手，格式自动对 |
| `校对报告.html` | 双击用浏览器打开。黄底是改动处，鼠标悬停看原文；左侧段号只是标尺，**复制文字不会带走它** |
| `修改清单.csv` | 双击用 Excel 打开。逐条列出第几段第几句改了什么、前后对照 |

### 段号怎么对应

`分段正文.txt` 里第 N 个段落，就是校对报告里的第 N 段，也是清单里「段号 = N」的那几条。
三份文件用同一根坐标轴，改完回来复制的永远是干净正文。

## 三之二、想秒出？用网页版（推荐日常使用）

嫌 Agent 跑得慢、不想每次都开对话框等算？直接用 `网页版.html`：

1. 双击 `网页版.html` 用任意浏览器打开（Chrome / Edge / 360 都行）
2. 把正文粘进去，选好标题风格、段落节奏，点「开始分段」
3. 同款算法在**你电脑本地**跑，不联网、不上传，标题 / 分段 / 标点 / 清单一次出齐
4. 标题可直接在框里改；纯净正文、清单都能一键复制

网页版和脚本用的是同一套逻辑：自动补全多段对话引号、补残缺引号、半角转全角、
提炼≤10字标题、人名笔误检测。日常校对这种本地跑最快，需要 AI 判断「这句话到底该不该断」时再用 Agent 技能。

## 四、觉得段落太碎或太长？

直接跟 AI 说「段落再长一点」即可，它会调参数重跑。手动调用的话：

```bash
python scripts/fenduan.py --原文 原文.txt --修订稿 修订稿.txt --输出目录 . --合并上限 32 --短句阈值 12
```

| 参数 | 默认 | 调大的效果 |
|---|---|---|
| `--短句阈值` | 6 | 更多短句被合并，段落变少 |
| `--合并上限` | 24 | 合并后允许更长，段落变少 |
| `--段落上限` | 45 | 放宽「偏长段落」的报警线 |
| `--最多逗号` | 2 | 允许更长的复句留在同一段 |

所有参数都有英文别名（`--short-max` `--merge-max` `--para-max` `--max-comma`），
中英文写法都能用。

## 五、环境要求

- Python 3.6 及以上，**不需要安装任何第三方库**
- 如果电脑上没有 Python，技能会自动改用纯手工模式，直接在对话里输出分段结果和
  Markdown 修改清单，功能不缺，只是没有 HTML 和 Excel 文件

## 六、它不会做什么

- 不改情节、不润色文笔、不增删描写
- 不擅自统一人名（可能是同族兄弟，只会标出来让你确认）
- 不动方言、口癖、网文黑话和作者刻意的重复

每次跑完都有一道自检：剥掉所有标点只比文字，一旦发现内容被改动过就会报警。

# 小说分段助手 v1.0.0

首个公开发布版本（MIT License）。

## 这个 Skill 做什么

网络小说正文自动分段 + 错别字与标点校对 + 标题提炼。贴一段正文（什么都不用说）即自动：
分段（一句一段为主、对话独立成段）、标点升级、纠错别字、生成 ≤10 字章标题，
并产出 `分段正文.txt` / `校对报告.html` / `修改清单.csv` 三份交付物。

## 三种用法

1. 直接粘贴正文，自动处理；
2. 双击 `网页版.html` 本地秒出（可接 DeepSeek / 智谱 GLM / Kimi 等做 AI 语义校对）；
3. 跑 `scripts/fenduan.py` 批量处理（仅 Python 标准库）。

## 包含内容

- `SKILL.md`、`安装说明.md`
- `网页版.html`（纯本地网页版）
- `scripts/fenduan.py`
- `references/错别字表.md`、`references/分段细则.md`

## 安装方法（作为 Skill）

将本仓库（或下方附件 zip）整体复制到：

- 用户级：`C:\Users\<你的用户名>\.workbuddy\skills\novel-segmenter\`
- 项目级：`<工作目录>\.workbuddy\skills\novel-segmenter\`

## Release 附件

`novel-segmenter.zip`：可直接解压使用的完整 skill 文件夹（含网页版与脚本）。

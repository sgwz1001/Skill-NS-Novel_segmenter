---


name: 小说分段助手
description: "网络小说正文自动分段 + 错别字与标点校对 + 标题提炼。把大段堆砌的长文本拆成符合网文快节奏阅读习惯的短段落（一句一段为主），修正不规范的逗号/句号、纠正错别字、补全/规范引号、半角标点转全角，并自动总结一个不超过 10 字的标题（网文常见 / 日式轻小说 / 古典章回体 / 国外 四种风格）。输出可粘贴进起点『作家助手』的纯净正文、带段号高亮的校对报告、逐条修改清单。另有纯本地网页版（网页版.html）与可接入 DeepSeek / 智谱 GLM / Kimi 等大模型 API 做语义校对的模式。当用户只粘贴了一段中文小说/网文正文而没有提出其他问题或指令时，自动使用本技能；用户说『分段』『断句』『排段落』『校对错别字』『整理成网文格式』『起个标题』『规范标点』时同样使用。"
license: MIT
agent_created: true
---

# 小说分段助手

把「一逗到底」的长段落，改造成网文读者习惯的短段节奏，同时把错别字和标点问题挑出来。

## 触发方式

**主要场景：用户在对话框里只粘贴了一段正文，什么都没说。** 这就是完整指令，不要反问「你想让我做什么」，直接按下面的工作流处理。

其他触发词：分段、断句、排段落、校对、纠错别字、整理成网文格式、作家助手格式。

**例外**：如果用户粘贴文本的同时提出了别的问题（比如「帮我看看这段写得怎么样」），那是写作点评需求，不要套用本技能。

## 交付物（固定三份）

| 文件 | 用途 |
|---|---|
| `分段正文.txt` | **纯净正文，无任何序号标记**。用户直接全选复制，粘贴进作家助手即可 |
| `校对报告.html` | 带段号的对照视图，改动处黄底高亮，鼠标悬停显示原文。段号用 CSS 生成，**复制文字时不会被带走** |
| `修改清单.csv` | Excel 可直接打开，逐条列出「第几段第几句、改了什么、为什么改」 |

段号即"坐标轴"：`分段正文.txt` 里第 N 个段落，就是报告里的第 N 段。用户核对完报告，回到纯净正文复制，两边严格对齐。

## 工作流

### 第 1 步 · 落盘原文

在当前工作目录建 `分段输出/<年月日-时分>/`，把用户粘贴的原文**一字不改**写入 `原文.txt`（保留原有换行）。

### 第 2 步 · 生成修订稿（这一步由你亲自做，不要交给脚本）

通读 `原文.txt`，做三类修改，产出 `修订稿.txt`：

1. **标点升级**：把该断句而作者偷懒用了逗号的地方，改成句号（详见下方规则）
2. **错别字纠正**：明确的用字错误（详见 `references/错别字表.md`）
3. **提炼标题**：用一句话概括本章主要内容，写成一个**不超过 10 个字**的标题。
   - 风格四选一（用户没指定就走「网文常见 / 简明」）：
     - `网文常见`（简明）：单句提炼，如「周明博的惊疑」
     - `轻小说`：日式轻小说风，短语外加「」包裹，如「「命运的齿轮开始转动」」
     - `章回体`：双段式「短语 短语」，如「明博惊疑 瑞龙陈情」
     - `国外`：西式章节标记，如「CH.01 · 周明博的惊疑」
     - `自动`：默认网文常见，最稳妥
   - 标题是给作家助手当章名的，宁可短而准，不要超过 10 字。

随后把标题通过脚本参数 `--标题 "你的标题"` 传给引擎（不传则引擎用内置启发式兜底，网页版同款逻辑）。

**铁律**：
- 不增删任何情节、描写、修饰词，不改写句式，不润色文笔
- 逐段对应输出，保留原有的自然段换行
- 拿不准的一律不改，交给第 4 步标为「待确认」
- **引号、半角标点不用你管**：脚本会自动补全多段对话的引号、补上残缺后引号、把半角逗号句号转成全角。你只需专心做标点升级和错字。
- 长文按原始自然段分批处理，每批 1500～2500 字，追加写入，避免一次输出过长导致丢字

### 第 3 步 · 跑分段引擎

```bash
python scripts/fenduan.py --原文 原文.txt --修订稿 修订稿.txt --标题 "周明博的惊疑" --标题格式 简明 --输出目录 .
```

（可执行文件名按环境试 `python` / `python3` / `py`。脚本只用 Python 标准库，无需装任何依赖。）

**想要秒出、不想等 Agent 慢慢算？** 直接用本技能自带的 `网页版.html`：双击在浏览器打开，粘贴正文，点「开始分段」，同款算法纯本地跑，标题/分段/标点/清单一次出齐，不联网、不上传。下面所有开关（标题风格、段落节奏滑块、补全引号、半角转全角、纠常错字）网页版都有，还带绝区零风格 UI、日夜切换、加载动画与翻译按钮。

**想让 AI 做语义判断（更准的标点升级与错字）？** 网页版右栏「绳匠终端」可选服务商（DeepSeek / 智谱 GLM / Kimi / 通义千问 / 豆包 / 混元 / 文心 / MiniMax）、选模型、填 API Key，勾选「启用 AI 语义校对」即可。同一份正文先由大模型返回修订稿，再走本地引擎分段、高亮、出清单；关掉开关则纯本地。翻译按钮复用同一接口把正文译成中文/日/法/韩等。

脚本自动完成：按句末标点切分 → 长短句合并 → 对话独立成段 → 编号 → 与原文逐字比对生成清单 → 输出三份产物。

**必须看脚本的终端输出**，它会报告三类问题：

- `体检警告` — 提示「疑似增删内容」说明第 2 步改过头了，回去修正 `修订稿.txt` 重跑
- `偏长段落` — 这些段的标点升级没做到位，回第 2 步重新判断这几段
- `待确认的专有名词` — 脚本发现只差一个字的高频/低频名字对，多半是人名笔误

### 第 4 步 · 补充语义标注（可选但推荐）

脚本只能判断"字变了"，判断不了"为什么变"。改动条数不多时（≤50 条），把修改原因补进 `修改清单.csv` 的「说明」列，例如：
- `的/地/得 用法`
- `长句拆分，前后动作独立`
- `标点升级：前后主语不同`

对脚本报出的「待确认专有名词」，在回复里单独提醒用户，例如：
> 注意：全文「周明博」出现 5 次，「周云博」出现 1 次，疑似笔误，已标出未改动。

专有名词（人名、地名、功法名）**只标记，绝不擅自统一**——有可能是同族兄弟或易容设定。

### 第 5 步 · 交付

向用户展示三份文件，并在回复里直接给出分段后的正文（用代码块包裹，方便一键复制），附一句话统计：原文 X 字 / Y 段 → 现在 Z 段，共修改 N 处。

## 分段规则（核心）

### 切段依据
以 `。` `！` `？` `……` 作为段落边界。句末标点后若紧跟收尾引号 `”` `」` `』`，一并归入本段。

### 长度控制
- **默认一句一段**，这是网文的基本节奏
- 单段建议 8～28 字
- 硬上限 45 字或 3 个逗号，超出必须再拆
- 「两个逗号 + 一个句号」已算长句，整段文本里应当是少数
- 极短句（≤6 字，如"他愣住了。"）可与下一句合并，但合并后不得超过 24 字；若本身自带情绪重量（"他死了。"），保持独立更有力

### 硬性独立成段
- 对话（以 `“` 开头）必须单独成段，不与叙述混排
- 场景切换、时间跳跃的句子单独成段
- 心理活动与外部动作描写不混段

### 保留原结构
用户原文里已有的换行是"大段边界"，分段只在大段内部进行，不跨越原有段落合并。

## 标点升级规则

判断某个逗号该不该升级成句号，看三条：

1. **前后分句各自主谓完整**、能独立成句 → 升级
2. 逗号后跟 `于是` `然后` `接着` `但是` `可是` `不过` `所以` `随后` `紧接着` 等连词，且前后语义可切分 → 升级
3. 逗号前后是**不同主语**在做**不同动作** → 升级

**不升级**的情况：
- 并列成分（"红的、绿的、黄的花"）
- 状语前置（"在那个雨夜，他离开了"）
- 转折关系紧到断开就伤义（"不是不想，是不敢"）

例：

> 原文：周明博抬头看向赵瑞龙，眼神中带着惊讶，看样子这和他们最开始的想法完全不一样。

> 处理后：
> 周明博抬头看向赵瑞龙。
> 眼神中带着惊讶。
> 看样子这和他们最开始的想法完全不一样。

## 引号与全半角规则（脚本自动处理，这里说明判定标准）

1. **引号完整性**：一句话若以 `“` 开头却没 `”` 收尾，脚本会补上后引号并警告。
2. **多段对话的引号习惯**：作者常把一长段对话只首尾加引号。按中文规范，**跨段引号每段开头都要有前引号，只有最后一段结尾有后引号**——脚本自动给中段补上前引号。
3. **全角 / 半角标点**：OCR、手机、网页复制常混入半角 `, . ! ? ; : " ( )`。脚本在标点旁有中文字时才转成全角，避免误伤数字（如 `3.14`）和英文。

## 纠错边界

**改**：明确的同音/形近错字（的地得、在再、做作、他她它、需须、账帐、竟竞、辨辩、以已、即既）、明显的标点误用（英文标点混入、句末缺标点、引号不成对）、半角标点转全角、常见错词（帐号→账号、布署→部署等，仅整词替换）。

**不改**：方言与口语化表达、作者刻意的重复与短句、生造词与网文黑话、人物口癖、专有名词。

**只标记不改**：专有名词前后不一致、疑似漏字、逻辑矛盾。

## 降级模式（目标应用不能跑脚本时）

若环境无法执行 Python，改为纯手工模式：

1. 按上述规则直接输出分段正文（代码块包裹，无序号）
2. 另起一段，输出 Markdown 表格形式的修改清单：段号 / 原文 / 改后 / 原因
3. 用 `**粗体**` 标出改动字词代替黄底高亮

段号依然按分段正文的段落顺序编排，保证能对上。

## 参数微调

用户觉得段落太碎或太长时，用脚本参数调整，不要改规则：

| 参数 | 默认 | 含义 |
|---|---|---|
| `--短句阈值` | 6 | 短于这个字数的段落才考虑向后合并，调大→段落变少 |
| `--合并上限` | 24 | 两个短句合并后的字数上限，调大→段落变长 |
| `--段落上限` | 45 | 单段字数硬上限，超出会被点名 |
| `--最多逗号` | 2 | 单段允许的逗号数 |
| `--标题` | 空 | AI 给定的标题；不填则引擎用启发式生成 |
| `--标题格式` | 自动 | `简明` / `章回体` / `自动` |
| `--标题上限` | 10 | 标题最多字数（硬上限，建议 10） |
| `--不转全角` | 关 | 默认开：半角标点转全角；加此开关可关 |
| `--不补全引号` | 关 | 默认开：补全多段对话引号与残缺引号；加此开关可关 |
| `--纠常错字` | 关 | 默认关：整词替换常见错字（避免误改，需时再开） |

每个参数都有英文别名（`--short-max` `--merge-max` `--para-max` `--max-comma` `--title` `--title-fmt` `--title-limit` `--fwhw` `--fix-quotes` `--fix-common`），
两种写法都能用，方便在不同 Agent 应用里调用。

## 参考资料

- `references/错别字表.md` — 网文高频错别字对照，按易混类型分组
- `references/分段细则.md` — 边界情况处理：对话夹叙述、省略号、破折号、数字与英文
